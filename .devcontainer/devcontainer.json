{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-python.python",
        "denoland.vscode-deno",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "deno.enable": true,
        "deno.lint": true,
        "editor.formatOnSave": true,
        "python.defaultInterpreterPath": "/usr/bin/python3",
        "terminal.integrated.defaultProfile.linux": "bash",
        "jupyter.notebook.defaultKernel": "deno"
      }
    }
  },
  "postCreateCommand": "curl -fsSL https://deno.land/install.sh | sh && export DENO_INSTALL=$HOME/.deno && export PATH=$DENO_INSTALL/bin:$PATH && deno jupyter --install --force && mkdir -p ~/.jupyter && echo '{\"kernel_name\": \"deno\"}' > ~/.jupyter/default-kernel.json || true",
}
